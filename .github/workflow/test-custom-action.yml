name: Test Custom Action

on: [push, workflow_dispatch]

jobs:
  run-custom-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run custom action
        uses: kovacstomi121/https://github.com/kovacstomi121/custom-action-challange.git